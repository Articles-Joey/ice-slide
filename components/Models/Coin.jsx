/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
Command: npx gltfjsx@6.5.3 Coin.glb -T 
Files: Coin.glb [13.81KB] > F:\My Documents\Sites games\ice-slide\public\models\Coin-transformed.glb [3.24KB] (77%)
*/

import React, { useRef } from 'react'
import { useGLTF } from '@react-three/drei'
import { useFrame } from '@react-three/fiber'

export function ModelCoin(props) {
  const { nodes, materials } = useGLTF('models/Coin-transformed.glb')
  const groupRef = useRef()
  const randomOffset = useRef(Math.random() * Math.PI * 2)

  useFrame((state) => {
    if (!groupRef.current) return
    const t = state.clock.getElapsedTime() + randomOffset.current
    // Rotate slowly
    groupRef.current.rotation.y = t * 0.5
    // Move up and down endlessly relative to the base position
    const baseY = props.position?.[1] ?? 0
    groupRef.current.position.y = baseY + Math.sin(t * 2) * 0.1
  })

  return (
    <group ref={groupRef} {...props} dispose={null}>
      <group rotation={[-Math.PI / 2, 0, 0]} scale={100}>
        <mesh geometry={nodes.Coin_1.geometry} material={materials.Yellow} />
        <mesh geometry={nodes.Coin_2.geometry} material={materials.DarkYellow} />
      </group>
    </group>
  )
}

useGLTF.preload('models/Coin-transformed.glb')
